# Core -- needed for the main pipeline and the ICLR interpretability case study
numpy==2.0.2
pandas==2.3.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
cvxpy==1.7.3
pyarrow==21.0.0

# Optional -- only needed for the monotone NN/GBM baselines
# (additional_models.py, monotone_nn.py, monotone_gbm.py, synthetic_simulations.py)
torch==2.8.0
xgboost==2.1.4
# mononet has no pip-installable pin verified here; install separately:
#   pip install "mononet[torch]"
